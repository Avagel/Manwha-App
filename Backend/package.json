{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for manwha app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npx playwright install-deps && npx playwright install chromium",
    "postinstall": "npx playwright install chromium"
  },
  "author": "Akanume Iruoghene",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "axios": "^1.12.2",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "^6.20.0",
    "mongoose": "^8.18.3",
    "playwright": "^1.55.0",
    "redis": "^5.8.2"
  }
}
